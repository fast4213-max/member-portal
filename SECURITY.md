# セキュリティと取り扱い方針

このアプリは組合員の**機微な個人情報**（氏名・生年月日・住所・電話・家族・共済加入・組合役員経験）を扱います。
管理する人・役員は、この文書を読んでから使ってください。

---

## 1. 守ること（基本ルール）

1. **リポジトリ（GitHub）は公開**です。プログラム以外は置かない
   - ❌ 名簿データ（スプレッドシートの中身・CSV・PDF・スクリーンショット）
   - ❌ パスワード、LINE のトークン、スプレッドシートやフォルダの ID
   - ❌ 組織名など、どの団体のものか分かる情報（組織名はスクリプトプロパティに置いている）
   - テストに使う名前・住所は架空のもの（例：山田 太郎・〒000-0000・〇〇県）だけ
2. スプレッドシートの共有は**「制限付き」のまま**。「リンクを知っている全員」にしない。ほかの人と共有しない
3. パスワードは口頭・紙・個別のメッセージで伝える。グループチャットや掲示物に書かない
4. 画面の共有・スクリーンショット・印刷物は個人情報として扱う（印刷した台帳は使い終わったらシュレッダー）
5. 役員の端末は画面ロックをかけ、使い終わったら［ログアウト］

## 2. 守りのしくみ（実装していること）

| 対策 | 内容 |
|---|---|
| 認証・権限はすべて GAS 側 | 画面側のチェックは飾り。GAS がすべての操作の最初にログインと権限（組合員／役員）を確かめ、権限のない操作は拒否して行動ログに残す |
| パスワード | スクリプトプロパティに**暗号化（SHA-256＋ソルト）した値だけ**を保存。比較は時間差で推測できない方式 |
| ログイン | 成功するとランダムな合言葉（トークン、6時間有効）を発行。ブラウザのタブを閉じると消える（sessionStorage） |
| 総当たり対策 | パスワードを5回続けて間違えると15分ロック（組合員用・役員用は別々）。本人確認（社員番号＋生年月日）は1回のログインで5回失敗するとログアウト、全体で1時間300回失敗すると一時停止 |
| 本人確認 | 一致しないときは「見つかりません」とだけ返し、どちらが違うかは教えない |
| 申請の識別 | 社員番号・生年月日そのものではなく、暗号化した値（requester_key）で申請を管理 |
| 台帳の直接変更の禁止 | 組合員は申請だけ。役員の承認で反映。承認待ちは1人最新1件、1日20回まで |
| 返す項目を最小に | 一覧は社員番号・氏名・フリガナ・年齢・更新日だけ。住所などは台帳を開いたときだけ返し、そのたびに行動ログに記録。全件を一度に取り出す機能はない（一覧は50件ずつ） |
| 行動ログ | 追記のみ（画面から編集・削除できない）。項目名だけを書き、住所・電話などの値は書かない |
| 入力チェック | GAS 側で必須・形式・文字数・選択肢を検査。スプレッドシートの数式として動かないよう、`=` `+` `-` `@` で始まる値は文字として保存 |
| 同時更新 | LockService で順番に処理。承認・編集の時点で台帳が変わっていたら止める |
| 画面の安全対策 | 入力された値は文字として表示（HTML として解釈しない）。読み込み先を制限（CSP）、検索エンジンに載せない（noindex）、参照元を送らない |
| 組織名 | コードに書かず、ログインした人にだけ GAS から渡す |

## 3. パスワードを変える

1. スプレッドシートを開き［台帳アプリ］→［② 共通パスワードを設定］または［③ 管理パスワードを設定］
2. 新しいパスワード（8文字以上）を2回入力
3. 変えた時点で、ログイン中の人は全員ログアウトになります
4. 新しいパスワードを必要な人に個別に伝える

> 役員は全員で1つの管理パスワードを使う方式です（行動ログの操作者はすべて `admin`）。
> 役員が交代・退任したら、**管理パスワードを変えて**新しい役員にだけ伝えてください。これが「役員の追加・失効」の手順です。

## 4. 漏えい・不正が疑われるとき

**すぐにやること（順番に）**
1. ［台帳アプリ］→［全員をログアウトさせる］
2. 漏れた可能性のあるパスワードを変更（共通・管理の両方を変えるのが安全）
3. 行動ログ（管理画面の［行動ログ］、またはシート `audit_log`）で確認
   - ログイン失敗・ロックが急に増えていないか
   - 見覚えのない時間帯の「詳細表示」「承認」「削除」「編集」がないか
   - 同じ申請者（#で始まる記号）からの大量の申請・却下の連続がないか
4. おかしな変更があれば、台帳表示の「変更履歴」→［戻す］、または README の「バックアップから戻す手順」で元に戻す
5. 組合の責任者に報告し、必要なら対象の組合員に連絡する

**ケース別**
| こんなとき | 対応 |
|---|---|
| スプレッドシートを誤って共有した | 共有をすぐ「制限付き」に戻し、共有相手を削除。ドライブの「アクティビティ」で閲覧状況を確認 |
| 管理者の Google アカウントが乗っ取られた疑い | Google アカウントのパスワード変更・2段階認証の確認。そのうえで上の1〜5 |
| GitHub に名簿やパスワードを誤って上げた | ファイルを消すだけでは履歴に残ります。すぐにパスワードを変更し、履歴からの削除を Claude などに依頼（リポジトリの非公開化も検討） |
| LINE のトークンが漏れた（後日 LINE 導入後） | LINE Developers でトークンを再発行し、［LINEの設定］で入れ直す |

## 5. 知っておくべき限界

- GAS は相手の IP アドレスを取得できないため、「どこから」のアクセスかは分かりません。抑止は承認制・申請の置き換え・回数制限・ロック・行動ログで行っています
- 共通パスワードを知っていれば、他人の社員番号と生年月日を当てようとすることはできます（回数制限とログで抑止）。社員番号と生年月日の組み合わせを、むやみに人に教えないよう周知してください
- 共通パスワードを5回続けて間違える人がいると、組合員全員が15分ログインできなくなります
- 画面の URL と GAS の URL は秘密ではありません（知っていてもパスワードがないと何もできない作りです）。ただし、むやみに広めないでください

## 6. 定期的に見直すこと

- [ ] 年に1回程度、共通パスワード・管理パスワードを変更
- [ ] 役員の交代時に管理パスワードを変更
- [ ] スプレッドシートとバックアップフォルダの共有が「制限付き」のまま
- [ ] 行動ログにおかしな動きがないか（月1回くらい）
- [ ] バックアップが毎日作られているか（「組合員台帳_バックアップ」フォルダ）
- [ ] 退会者の台帳をゴミ箱へ。90日たったら必要に応じて完全削除
