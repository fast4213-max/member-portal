# 作業ログ

組合員台帳アプリ（member-portal）の作業記録。新しいセッションはまずこれと `DESIGN.md` を読む。

## 現在地（2026-09-25 時点）

| 段階 | 状態 |
|---|---|
| 1. 設計（`DESIGN.md`） | ✅ ほぼ確定（v0.3＋追記） |
| 2. HTMLモック（デザインキャンバス） | ✅ 完成（本番画面に移植済み） |
| 3. GAS 実装 | ✅ 0.2.0（編集機能入り）をデプロイ済み。動作テスト22項目すべてOK（ユーザー確認） |
| 4. 画面（`docs/`）実装・GAS接続 | ✅ 公開済み：https://fast4213-max.github.io/member-portal/ （実機確認中） |
| 5. 承認フロー・ゴミ箱・バックアップ・LINE通知 | ✅ LINE以外は稼働。**LINE通知はユーザー判断で後日**（コードは line.gs にあり、設定しなければ何もしない） |
| 6. README / SECURITY.md・セキュリティ自己レビュー | ⬜ |

## 前提・ルール

- ユーザーへの説明は、専門用語を避けて手順を番号付きで分かりやすく

- ブランチは **main のみ**（ユーザー指示）。直接 push する
- リポジトリは公開。実在の個人情報・パスワード・シートID・LINEトークンは絶対にコミットしない（秘密はGASのスクリプトプロパティ）
- 元の依頼書：`member-portal-prompt.md`（アップロード資料。リポジトリには未収録）、台帳PDF（白紙様式、レイアウト参考のみ・`.gitignore` で *.pdf 除外）

## HTMLモック

- キャンバス：https://claude.ai/artifact/X3sy3hW33VY7is2kYmSzoZ （非公開。共有はページのShareメニューから）
- ソースの控え：`prototype/`（キャンバスの中身をそのまま保存）
  - `App.dc.html` … 本体。全画面を1ファイルの状態切替で実装（幅900pxでスマホ/PCレイアウト切替）
  - `Sp*.dc.html` / `Pc*.dc.html` / `Main.dc.html` … 本体を 390×844 / 1440×960 で表示する薄いラッパー（`start` で初期画面指定）
  - `app.css` … 共通CSS。キャンバスにはアセット `/_blob/e74f08885814685ddef69d85a054250e` としてアップロード済み（CSSを変えたら再アップロードして全ファイルのリンクを差し替える）
- 試作データ（架空）：社員番号 `1234567`＋生年月日 `1990年4月12日` が登録済み。パスワードは何でも通る
- 実装時の注意：台帳シート用の `.v` クラス（青インク色）と他の要素のクラス名がぶつかって色がおかしくなった → 本実装ではクラス名を分ける

## 決定事項まとめ（詳細は DESIGN.md）

- ログイン：共通／管理のタブ、**パスワードだけ**。管理パスは1つを共有（ログ上の操作者は `admin`）。5回失敗で15分ロック
- 組合員：メニュー → 新規 / 更新
  - 新規：社員番号＋生年月日を入れた時点で照会 → 登録済みなら「登録済みです」→ 更新へ誘導。未登録なら続きを入力（年齢は出さない）
  - 更新：社員番号＋生年月日が一致すると現在の内容入りフォーム。不一致は「見つかりません」のみ
  - 入力後は**確認画面＝台帳表示**。下の［この内容で申請する］で申請 → 「申請しました」
  - 文言：「間違えた場合は、もう一度申請すれば前回の申請は破棄され、新しい内容に更新されます」
  - **承認制**（申請 → 役員が承認して反映）。承認待ちは本人ごとに最新1件だけ
- 項目：氏名・フリガナ・家族の氏名は**姓／名で分割**。社員番号は半角7桁。数字は全角→半角自動変換。日付は年/月/日の3欄（月日は1でも01でも可、保存はゼロ埋め）。郵便番号は数字7桁でハイフン自動。**最寄駅（自宅）は必須**。職名は自由入力。続柄：妻・夫・子・父・母・義父・義母・その他。性別：男・女。家族は最大10人
- 管理（スマホ・PC両対応）
  - PC：左サイドバー（台帳一覧／承認待ち／行動ログ／ゴミ箱）
  - スマホ：下タブ4つ（台帳一覧／承認待ち／行動ログ／ゴミ箱）
  - 一覧：あ〜わ行ボタン＋氏名・カナ・社員番号検索。列は**社員番号・氏名・フリガナ・年齢・更新日**
  - 氏名クリック → 台帳表示（PDFと同じ見た目、［印刷（A4）］［ゴミ箱へ移動］）
  - 承認待ち：変更前／変更後の差分（黄色）、承認・却下（却下は理由必須）
  - 行動ログ：種類で絞り込み、新しい順、値は書かず項目名のみ
  - ゴミ箱：［復元］（家族も）／［完全削除］は削除から90日経過後のみ・管理パス再入力・実行前に自動バックアップ
- 台帳表示（スマホ）：［読みやすい表示］（縦並びカード・初期）と［台帳の形］（紙と同じ・拡大可）を2つのボタンで切替。更新時の変更箇所はどちらも黄色
- LINE通知：Webhookなし・pushのみ、GASの時間トリガーで1日1回「承認待ちがN件」だけ送る（後半で実装）

## GAS 実装メモ

- スプレッドシートに紐づいたスクリプト（コンテナバインド）。スプレッドシートに「台帳アプリ」メニューを出し、初期設定・パスワード設定・動作テスト・LINE設定をメニューから行う（値はスクリプトプロパティへ。パスワードはハッシュのみ保存）
- ファイル：Code（入口・ルーティング）/ schema（項目定義・検証）/ db（シート読み書き）/ auth / audit / members / requests / trash / backup / line / setup（メニュー）/ test（動作テスト）
- 全セルを書式なしテキストで保存。先頭 = + - @ は ' を付けて保存し読むときに外す
- ログインの有効時間は6時間（CacheService の上限）
- 承認時の衝突：申請後に台帳が変わっていたら stale で止める → 役員が見比べて `ack_stale` と `seen_updated_at` を付けて再承認
- 完全削除は台帳・家族・申請・履歴からその人を消す（消した記録だけ履歴に残す）。事前に自動バックアップ
- 検証：ローカルで GAS を模擬して動作テスト（test.gs の runSelfTest_）を実行し 21項目すべてOK。ログイン5回失敗ロック・権限拒否も確認
- 残り：LINEの groupId の取り方（Webhookを使わない方法）を README に書く、CSVエクスポートは未実装（必要なら追加）

## GAS の本番URL（公開情報・秘密ではない）

- `https://script.google.com/macros/s/AKfycbx6gf6dE7ayikTDhv0YjtbFQttTdQATmNY6iR2MgDwefQoju31LbPi28r_QizNoEdZu/exec`
- 確認済み（0.1.0 時点）：ping → version 0.1.0 ／ ログインなしの listMembers → auth で拒否 ／ 不明な action → bad_action
- コードを直したら「デプロイを管理 → 編集 → 新バージョン」で同じURLのまま更新（ユーザーに依頼）
- 未確認：バックアップ（メニュー［今すぐバックアップ］）、LINE通知（設定前）
- GAS を変えたときにユーザーへ伝えること：変わったファイル名、貼り直し → ［デプロイを管理］→ ✏ →［新バージョン］（［新しいデプロイ］はURLが変わるので不可）、［④ 動作テスト］と［⑤ Web接続テスト］でバージョン確認
- **シートの列（`schema.gs` の `SHEETS`）を変える修正は、事前にユーザーへ伝え、既存データの移行手順も用意する**（ユーザーからの質問「GASを変えるとスプシがずれないか」への約束）。列を変えない修正ならデータはずれない

## 画面（docs/）実装メモ

- ビルドなしの素の JS。Tailwind は使わず、モックの CSS（`prototype/app.css`）を移植した `assets/style.css`（幅900pxで PC/スマホ切替を media query に変更）
- ファイル：`index.html` / `assets/api.js`（GAS_URL・fetch）/ `ui.js`（要素作成 h()・アイコン・入力整形・画面側チェック）/ `ledger.js`（台帳：紙の形・読みやすい表示・印刷）/ `app.js`（土台・ログイン・メニュー）/ `member.js` / `admin.js`
- XSS対策：値はすべて textContent。innerHTML は固定のアイコンSVGだけ。CSP・noindex・no-referrer を index.html に設定
- トークンは sessionStorage のみ
- 入力欄から離れるたびに全体を描き直すと入力が消える・スマホのキーボードが閉じるので、エラー表示はその場で書き換える方式にした
- モックからの修正：台帳の `.v` → `.lv`、ボタン `.btn.sec` と区切り `.sec` の衝突を上書きで解消、携帯番号は11桁ならハイフン自動
- 検証：ローカルで GAS を模擬（gas/*.gs をそのまま実行）し、Playwright で スマホ／PC の 新規→承認→更新→差分承認→削除→復元→ログ→印刷（A4 1ページ）まで通し、エラーなし

## 追加（2026-09-25）

- 役員の「編集」：台帳表示の［ゴミ箱へ移動］と［印刷］の間。承認なしで直接反映、変更前は履歴（reason=edit）に残り［戻す］可能。開いたあとに更新されていたら stale で止める（GAS `updateMember`、行動ログ action=edit）→ GAS 0.2.0（要：貼り直し＋新バージョンでデプロイ）
- 台帳の家族欄：氏名欄を「上にフリガナ・点線・下に氏名」に。見出しの高さずれ（性別・続柄・同別の折り返し）を修正
- 承認待ちの詳細：読み込み失敗時に理由と［再読み込み］を表示（本番で「何も表示されない」との報告。模擬環境では再現せず）
- お知らせ（トースト）は画面上部に表示
- GitHub Actions の Node.js 20 警告：GitHub 側の自動ワークフロー（pages-build-deployment）のもの。公開は成功しており影響なし。消すには自前の Actions が必要だが、依頼書で「Actions は使わない（必要なら相談）」なので、おすすめは「そのまま」と伝えて返事待ち

## 次にやること

1. ユーザー：本番で承認待ちの詳細が出るか確認（赤いエラーが出たらスクショをもらって原因調査）
2. README / SECURITY.md、セキュリティ自己レビュー（秘密の混入・権限チェック・返す項目・XSS・数式インジェクション・置き換え/回数制限・git log）
3. （後日・ユーザーから声がかかったら）LINE通知の設定手順（Webhookを使わない groupId の取り方）

## 履歴

- 2026-09-24
  - PDF様式を読み取り、設計書 `DESIGN.md` 作成（main を新規作成して push）
  - 質問への回答を反映（承認制、管理パス共有、分会固定、main のみ、社員番号7桁、一覧の列 など）
  - デザインキャンバスでHTMLモック作成 → 指摘を受けて改修
    - 姓名分割、郵便番号ハイフン自動、最寄駅（自宅）必須、未登録時の年齢非表示
    - 確認画面を台帳表示に、申請後の文言追加
    - 全画面スマホ・PC両対応（1つの本体をスマホ幅／PC幅で表示）
    - スマホの台帳表示に「読みやすい表示／台帳の形」切替
    - 承認待ちカードの数字の色の不具合を修正、ゴミ箱画面（復元・完全削除）を追加
  - モックのソースを `prototype/` に保存、本ログ作成
  - GAS 実装（`gas/`）：認証・申請/置き換え/承認/却下/取消・一覧/詳細・履歴/戻す・ゴミ箱/完全削除・行動ログ・バックアップ・LINE通知・メニュー・動作テスト。手順書 `gas/SETUP.md`
- 2026-09-25
  - ユーザーが GAS をセットアップ：動作テスト全部OK、Webアプリとしてデプロイ。こちらから ping・認証拒否を確認
  - 画面（`docs/`）の実装に着手
  - 画面（`docs/`）を実装。模擬GAS＋ブラウザで全画面の通し確認、見た目の不具合（トーストの重なり・ボタン崩れ）を修正
- 2026-09-25（続き）
  - ユーザーが GitHub Pages を公開（https://fast4213-max.github.io/member-portal/）
  - 指摘対応：役員の［編集］を追加（GAS 0.2.0）、家族欄のフリガナ枠を修正、承認待ち詳細の読み込み失敗を表示、お知らせを画面上部へ。GAS動作テスト22項目・ブラウザ通しテストともOK
  - Actions の Node.js 20 警告は GitHub 側のものと説明（自前 Actions 化は相談中）
  - 「GASを変えてもスプシはずれないか」→ 列定義は変えていないのでずれない、と回答。列を変えるときは事前に伝える約束
  - ユーザーが GAS 0.2.0 を貼り直し・デプロイ、動作テスト全部OK。LINE は後日実装と決定
  - 管理の一覧：承認待ちカードを他と同じ白背景・黒字にし、「→」を削除（押すと承認待ちへ移動するのはそのまま）
