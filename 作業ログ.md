# 作業ログ

組合員台帳アプリ（member-portal）の開発記録。新しい作業を始めるときは、まずこれと `DESIGN.md`・`README.md` を読む。

## 現在地（2026-09-26）

| 段階 | 状態 |
|---|---|
| 1. 設計（`DESIGN.md`） | ✅ 確定 |
| 2. 試作（デザインキャンバス） | ✅ 本番画面に移植済み（試作ソースはリポジトリから削除） |
| 3. GAS（`gas/`） | ✅ 0.3.2（速度改善）を本番に反映済み・動作テスト全部OK |
| 4. 画面（`docs/`） | ✅ GitHub Pages で公開中 |
| 5. 承認・ゴミ箱・バックアップ | ✅ 稼働中 |
| 6. LINE通知 | ⏸ 後日（ユーザー判断）。コードは `gas/line.gs` にあり、未設定なら何もしない |
| 7. README / SECURITY.md | ✅ 作成済み |
| 8. セキュリティ自己レビュー | ✅ 最終点検済み（下の「セキュリティ最終点検」）。Git の履歴に残る古い組織名は**ユーザー判断でそのまま**（履歴の書き換えはしない） |

## 作業のルール（ユーザーとの約束）

- ブランチは **main のみ**。直接 push する
- リポジトリは公開。**プログラム以外（名簿・パスワード・トークン・シートID・組織名など、どの団体か分かる情報）は置かない**。組織名はスクリプトプロパティ `ORG_*`
- テストデータは架空（山田 太郎・〒000-0000・〇〇県・〇〇線）
- ユーザーへの説明は、専門用語を避けて手順を番号付きで分かりやすく
- GAS を変えたら：変わったファイル名を伝える → 貼り直し → ［デプロイを管理］→ ✏ →［新バージョン］（［新しいデプロイ］はURLが変わるので不可）→［④ 動作テスト］と［⑤ Web接続テスト］でバージョン確認
- **シートの列（`gas/schema.gs` の `SHEETS`）を変える修正は、事前にユーザーへ伝え、既存データの移行手順も用意する**。列を変えない修正ならデータはずれない
- GitHub Actions・cron-job.org は使わない（必要なら相談）。Pages 公開時の「Node.js 20」警告は GitHub 側の自動処理のもので影響なし

## 公開情報（秘密ではない）

- 画面：https://fast4213-max.github.io/member-portal/
- GAS：`docs/assets/api.js` の `GAS_URL`（パスワードがないと何もできない）

## 決まっていること（要点。詳細は DESIGN.md）

- ログイン：共通／管理のタブ、**パスワードだけ**。管理パスは役員で1つを共有（操作者は `admin`）。5回失敗で15分ロック
- 組合員：新規（社員番号＋生年月日で登録済みを自動確認）／更新（本人確認 → いまの内容を直す）→ 確認画面は紙の台帳の形 → 申請
- 承認制。承認待ちは1人最新1件（前の申請は置き換え）、本人が取消可、1日20回まで。申請後に台帳が変わっていたら承認を止めて見比べ
- 役員：台帳一覧（あ〜わ行・検索、列は社員番号・氏名・フリガナ・年齢・更新日）、台帳表示（印刷A4・**編集**・ゴミ箱・変更履歴から戻す）、承認待ち（差分・却下は理由必須）、行動ログ、ゴミ箱（完全削除は90日後・管理パス再入力・事前バックアップ）
- 項目：姓名・フリガナは姓／名で分割、社員番号は半角7桁、日付は年月日の3欄（1でも01でも可）、郵便番号・携帯番号はハイフン自動、最寄駅（自宅）必須、家族は最大10人
- スマホの台帳表示は「読みやすい表示／台帳の形」を切替
- LINE通知：後日。Webhookなし・pushのみ・1日1回「承認待ちがN件」だけ
- 自宅・実家の電話番号はハイフンを自動で入れない（ユーザー判断：市外局番の桁数が地域で違い区切りを決められないため。入力どおり保存、ハイフンあり・なしどちらでも可）。携帯（070/080/090 の11桁）だけ自動
- 却下の取り消し機能は**付けない**（ユーザー判断：操作が増えると悪用の余地も増えるため）。間違えて却下したら本人に再申請してもらう。やむを得ないときは管理者が `change_requests` の status を pending に戻す（ログには残らない）

## 実装メモ

### GAS（`gas/`）
- スプレッドシートに紐づいたスクリプト。スプレッドシートの［台帳アプリ］メニューで初期設定・パスワード・組織名・動作テスト・バックアップ・LINE設定
- ファイル：Code（入口）/ schema（列・項目・検証・組織名）/ db（シート読み書き）/ auth / audit / members（本人確認・一覧・詳細・編集・履歴）/ requests（申請・承認）/ trash / backup / line / setup（メニュー）/ test（動作テスト22項目）
- 全セルを書式なしテキストで保存。先頭 `= + - @` は `'` を付けて保存し、読むときに外す
- ログインの有効時間は6時間（CacheService の上限）
- 承認・編集の衝突は updated_at で検出（承認は `ack_stale`＋`seen_updated_at` で見比べ後に再承認、編集は開き直し）
- 完全削除は台帳・家族・申請・履歴からその人を消す（消した記録だけ履歴に残す）

### 画面（`docs/`）
- ビルドなしの素の JS。見た目は試作の CSS を移植（幅900pxで PC／スマホ切替）
- ファイル：`index.html` / `assets/api.js`（接続先・組織名の受け取り）/ `ui.js` / `ledger.js`（台帳：紙の形・読みやすい表示・印刷）/ `app.js` / `member.js` / `admin.js` / `style.css`
- XSS対策：値はすべて textContent（innerHTML は固定のアイコンだけ）。CSP・noindex・no-referrer。トークンは sessionStorage
- 入力欄を離れるたびに画面全体を描き直すと入力が消える・スマホのキーボードが閉じるので、エラー表示はその場で書き換える
- 台帳（紙の形）の見出しセルは `height` ではなく `min-height` で指定する。固定すると隣のセルが折り返して行が高くなったとき見出しだけ伸びず、罫線が切れる
- 氏名は文字の種類を制限・変換しない（旧字体・異体字もそのまま保存。数字の全角→半角だけ）

### 検証のしかた
- GAS：スプレッドシートの［④ 動作テスト］（22項目）。開発中は GAS を手元で模擬して同じテストを実行
- 画面：模擬GAS＋ブラウザ（Playwright）でスマホ／PC の 新規→承認→更新→差分承認→編集→削除→復元→ログ→印刷（A4 1ページ）を通す

### セキュリティ最終点検（2026-09-25、GAS 0.3.1）
- 問題なし：全操作で GAS がログイン・権限を確認／内部エラーの中身は画面に返さない／値は textContent のみ（innerHTML は固定アイコンだけ）／リポジトリに秘密・組織名なし（GAS の URL は公開情報）／一覧は最小項目・全件取得なし／ログに値を書かない／数式インジェクション対策
- 直したこと
  - 失敗回数・申請回数の数え上げと行動ログの追記に排他をかけた（同時に大量に送られると数え漏れて回数制限をすり抜けられた・ログが上書きで消えることがあった）。`withLock_` は入れ子で呼べるようにした
  - 申請の全体上限（1時間200回）。共通パスワードを知っていれば社員番号を変えて申請を大量に出せたため
  - 画面：30分操作がなければ自動ログアウト、iframe の中では開かない
- 残る限界（SECURITY.md に記載）：管理パスワードのロックを他人に起こされうる、ZipCloud に郵便番号だけ送る、パスワードは SHA-256＋ソルト（変えるとパスワード再設定が必要なので据え置き）

### 速度改善（2026-09-25、GAS 0.3.2）
- 「登録や一覧がもっさり」への対応。1回の操作にかかる時間の大半は GAS の起動と往復（1〜2秒）で、これは GAS の仕組み上なくせない。減らせたのは次のとおり
- GAS：同じシートを1回の実行で何度も読み直していたのを1回に（`readAll_` のキャッシュ。書き込んだシートと排他を取った時点で捨てる）。スクリプトプロパティもまとめて1回で読む。承認待ち一覧は件数が増えるほど遅くなっていた（1件ごとに台帳・家族を読み直し）のを解消。ログは期間の行だけ読む
- GAS：`identify` が申請の状況も返す（更新の本人確認が2往復→1往復）。`myRequests` は旧画面用に残す
- 画面：ページを開いた時点で GAS を起こしておく（`ping`）、承認・却下のあと次の申請を一覧の読み直しと同時に開く

## 次にやること

1. 組織名を設定済みか確認（画面でログインし直して、台帳の見出しに出るか）
2. 本番で承認待ちの詳細が出るか確認（赤いエラーが出たらスクショをもらう）
3. （後日）LINE通知の設定手順

## 履歴

- 2026-09-24
  - 台帳PDF（白紙様式）を読み取り、設計書を作成。質問への回答を反映（承認制・管理パス共有・main のみ・社員番号7桁・一覧の列 など）
  - デザインキャンバスで試作 → 指摘を受けて改修（姓名分割・郵便番号ハイフン自動・最寄駅必須・確認画面を台帳の形に・スマホ／PC両対応・読みやすい表示の切替・ゴミ箱）
  - GAS を実装（認証・申請/置き換え/承認/却下/取消・一覧/詳細・履歴/戻す・ゴミ箱・行動ログ・バックアップ・LINE・メニュー・動作テスト）、手順書 `gas/SETUP.md`
- 2026-09-25
  - ユーザーが GAS をセットアップ・デプロイ（動作テスト全部OK）
  - 画面（`docs/`）を実装し GitHub Pages で公開
  - 指摘対応：役員の［編集］（GAS 0.2.0）、家族欄のフリガナ枠、承認待ち詳細の読み込み失敗表示、お知らせを画面上部へ、承認待ちカードを白背景に
  - LINE通知は後日に決定
  - README・SECURITY.md を作成。組織名をコードから外してスクリプトプロパティへ（GAS 0.3.0）、テストデータ・入力例を地名の分からない架空値に、試作ソース（`prototype/`）を削除、作業ログを整理
  - 過去の Git 履歴に残る組織名は、ユーザー判断でそのままに（強制 push は環境の安全設定でも止められた）
  - ユーザーが GAS 0.3.0 を反映（動作テスト全部OK）。本番 ping で 0.3.0 を確認
  - セキュリティ最終点検と修正（GAS 0.3.1：排他・申請の全体上限／画面：無操作30分で自動ログアウト・iframe 対策）。模擬GASの動作テスト22項目・ブラウザ通しテスト・自動ログアウトのテストが通過
  - ユーザーが GAS 0.3.1 を反映（動作テスト全部OK）。組合員用・役員用の説明書PDFを作成（リポジトリには入れない）
  - 却下の取り消し機能は付けないと決定
  - 速度改善（GAS 0.3.2・画面）。模擬GASの動作テスト22項目・ブラウザ通しテストが通過
  - ユーザーが GAS 0.3.2 を反映（動作テスト全部OK）
- 2026-09-26
  - 台帳（紙の形）の修正（画面のみ・GAS変更なし）：社員コード欄の罫線切れを直した（見出しセルを `min-height` に）、最寄駅を「自宅」「実家」の2段表示にし、長い路線名は中身だけ折り返す。ユーザーが本番で確認OK
  - 最寄駅：入力に付けた「線」「駅」を自動で外す（入力欄の後ろに「線」「駅」を表示。すでに登録済みの「〇〇線」も台帳では二重にならない）。台帳の家族欄で生年月日・続柄が2段になるのを直した（列を少し広げ1行に）。画面のみ・GAS変更なし。ブラウザ通しテスト通過、main に反映済み
  - すでに「〇〇線」「〇〇駅」で登録済みの人への対応（データはそのまま・GAS変更なし）：更新・編集フォームでは線・駅を外して表示、線・駅を外しただけでは「変更あり」・黄色・承認待ちの差分に出さない。ブラウザ通しテスト通過
  - 自宅の電話番号にハイフンを自動で入れるか相談 → いまのまま（入力どおり）に決定。コード変更なし
