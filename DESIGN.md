# 組合員台帳アプリ 設計書（ドラフト v0.1）

> ステータス：**一部確定**（決定事項は「0. 決定事項」、残りは「8. 確認したいこと」）

## 0. 決定事項

| 項目 | 決定 |
|---|---|
| 新規・更新の反映 | **承認制**（組合員の送信は申請 → 役員が承認して台帳に反映） |
| 管理ログイン | **管理用パスワード1つを共有**（ログ上の操作者は `admin` 固定。誰が操作したかは区別しない） |
| 支部・分会 | **固定**：大阪地方本部 天王寺支部 天王寺車掌区分会（表示のみ） |
| ブランチ | **main のみ**。GitHub Pages も main の `/docs` から公開 |

> 元資料：組合員台帳PDF（白紙様式）、member-portal-prompt.md

---

## 1. 全体構成

```
[スマホ/PC ブラウザ]
      │  URLを開く（GitHub Pages：静的HTML+JS）
      ▼
[docs/index.html + assets/*.js]
      │  fetch POST（Content-Type: text/plain・JSON本文）
      ▼
[GAS Webアプリ（doPost）]  ← 認証・権限チェック・入力検証はすべてここ
      │
      ├─▶ [Googleスプレッドシート]（members / family / change_requests / members_history / audit_log）
      └─▶ 時間トリガー（1日1回）→ LINE Messaging API push（「承認待ちがN件」だけ）
```

- 画面：GitHub Pages（`main` ブランチの `/docs` を公開）。Tailwind CDN＋素のJS。ビルドなし
- API／DB：Google Apps Script＋スプレッドシート（共有は「制限付き」のまま）
- 秘密の値（パスワード・シートID・LINEトークン等）は**すべてGASのスクリプトプロパティ**。リポジトリにはコードだけ

---

## 2. 画面遷移

```
                 ┌──────────────── ログイン画面 ────────────────┐
                 │  [ 共通 ] タブ / [ 管理 ] タブ                    │
                 │  パスワードだけ入力 → ログイン                    │
                 └──────────┬───────────────────┬─────────────┘
                     共通パス│                   │管理パス
                            ▼                   ▼
               ┌── 組合員メニュー ──┐     ┌──── 管理画面 ────────────────┐
               │ [ 新規 ] [ 更新 ]   │     │ タブ：[一覧] [承認待ち] [行動ログ] │
               └───┬────────┬───┘     └──────┬───────────────────────┘
                   │        │                    │ 氏名クリック
                   ▼        ▼                    ▼
            新規登録フォーム   本人確認              台帳表示（PDFと同じ見た目）
            （社員コード＋     （社員コード＋         ［印刷］［編集］［削除］
             生年月日を入れた   生年月日）
             時点で重複チェック   │一致
             →「登録済みです」）  ▼
                   │        更新フォーム（現在の内容入り）
                   ▼        ▼
                   送信完了（申請受付）
```

### 2-1. ログイン画面
- タブ切替「共通」「管理」。どちらも**パスワード入力欄だけ**
- 共通：スクリプトプロパティ `MEMBER_PASS`（ハッシュ）と照合 → role=member のトークン発行
- 管理：スクリプトプロパティ `ADMIN_PASS_HASH`（SHA-256＋ソルト）と照合 → role=officer、操作者は `admin`
  - パスを変えたい時はスクリプトプロパティを書き換えるだけ。漏えい時は変更＋全トークン無効化
- 5回連続失敗で15分ロック（GAS側）

### 2-2. 組合員メニュー
- 大きなボタン2つ：**新規登録** / **登録内容の更新**
- 補助リンク：申請状況の確認・取消（更新画面の中に表示）

### 2-3. 新規登録フォーム（紙の台帳と同じ並び）
1. 最初に **社員コード＋生年月日** を入力
   - 両方そろった時点（入力欄から離れた時）でGASに照会
   - 登録済み → 「**登録済みです。『更新』から変更してください**」を表示し、［更新へ進む］ボタン。以降の入力欄はロック
   - 未登録 → 残りの項目を入力可能に
2. 基本情報 → 電話 → 現住所 → 実家住所 → 最寄駅 → 家族構成 → 共済 → 前職場 → 組合役員経験
3. 確認画面 → 送信

### 2-4. 更新フォーム
1. 本人確認：社員コード＋生年月日
   - 一致 → 現在の登録内容が入ったフォームを表示
   - 不一致 → 「見つかりません」（どちらが違うかは言わない・失敗回数に数える）
2. 変更して送信 → 変更箇所を確認画面でハイライト → 送信

### 2-5. 管理画面
- **一覧タブ**
  - 行ボタン：［すべて］［あ］［か］［さ］［た］［な］［は］［ま］［や］［ら］［わ］（フリガナ先頭で絞り込み）
  - 氏名検索（漢字・カナどちらでも部分一致）
  - 表の列：社員番号 / 氏名 / フリガナ / 年齢 / 更新日 （住所・電話は一覧に出さない）
  - 50件ずつページング
  - **氏名クリック → 台帳表示**（PDFと同じレイアウトのHTML。A4印刷用CSS付き）
- **承認待ちタブ**：変更前/変更後の差分ハイライト、承認・却下
- **行動ログタブ**：日時 / 操作者 / 操作 / 対象 。期間・操作種別で絞り込み、新しい順50件ずつ

---

## 3. 項目定義（PDF → データ項目）

### 3-1. 本人（シート `members`・1人1行）

| PDF上の項目 | 項目ID | 入力形式 | 必須 | 備考 |
|---|---|---|---|---|
| （作成日） | created_at / updated_at | 自動 | - | 台帳表示の「年月日作成」に updated_at を表示 |
| 地方本部 | honbu | 固定値 | - | 「大阪地方本部」 |
| 支部 | branch | 固定値 | - | 「天王寺支部」 |
| 分会 | bunkai | 固定値 | - | 「天王寺車掌区分会」 |
| フリガナ（セイ／メイ） | sei_kana / mei_kana | 全角カナ | ○ | セイの先頭であ行〜わ行を絞り込み |
| 氏名（姓／名） | sei / mei | テキスト | ○ | 姓と名は別の欄 |
| 性別 | gender | 男/女 選択 | ○ | |
| 年齢 | （保存しない） | 自動計算 | - | 生年月日から表示 |
| 生年月日 | birth_date | 日付 | ○ | 本人確認キー |
| 職名 | job_title | 自由入力 | ○ | |
| 契約社員入社年月日 | contract_join_date | 日付 | | 該当者のみ |
| 正社員入社年月日 | regular_join_date | 日付 | | |
| 電話（自宅） | tel_home | 電話番号 | | |
| 電話（携帯） | tel_mobile | 電話番号 | ○ | |
| 現住所 〒 | zip | 数字7桁（ハイフンは自動で入る） | ○ | 保存は 000-0000。郵便番号→住所の自動補完（ZipCloud） |
| 現住所 | address | テキスト | ○ | |
| 実家住所 〒 | family_zip | 数字7桁（ハイフンは自動で入る） | | |
| 実家住所 | family_address | テキスト | | |
| 実家電話 | family_tel | 電話番号 | | |
| 社員コード | employee_code | 半角数字7桁 | ○ | 本人確認キー |
| 最寄駅 自宅（線・駅） | station_home_line / station_home | テキスト | ○ | |
| 最寄駅 実家（線・駅） | station_family_line / station_family | テキスト | | |
| 総合共済 | kyosai_sogo | 加入済/未加入/不明 | ○ | ラジオ |
| 救援共済 | kyosai_kyuen | 加入済/未加入/不明 | ○ | ラジオ |
| 交通政策をすすめる会 | kotsu_seisaku | 加入済/未加入/不明 | ○ | ラジオ |
| 家族支援共済 | kyosai_kazoku | 加入済/未加入/不明 | ○ | ラジオ |
| 前職場 | prev_workplace | テキスト | | |
| 組合役員経験 | officer_exp | 無/有 | ○ | |
| └ 以前/現在 | officer_when | 以前/現在 | 有の時○ | |
| └ 所属箇所と役職 | officer_detail | テキスト | 有の時○ | |
| （管理用） | member_id, updated_by, deleted, deleted_at, deleted_by | 自動 | - | |

### 3-2. 家族（シート `family`・1人1行、member_idで紐づけ）

| PDF上の項目 | 項目ID | 入力形式 |
|---|---|---|
| フリガナ（セイ／メイ） | sei_kana / mei_kana | 全角カナ（セイは本人と同じ値を初期入力） |
| 氏名（姓／名） | sei / mei | テキスト（姓は本人と同じ値を初期入力） |
| 生年月日 | birth_date | 日付 |
| 性別 | gender | 男/女/その他 |
| 続柄 | relationship | 選択（妻/夫/子/父/母/義父/義母/その他） |
| 同別 | living_together | 同居/別居 |

- 「＋家族を追加」で行を増減。PDFの枠は **最大10人**（5行×2列）なので上限10人

### 3-3. その他のシート
- `change_requests`（申請）・`members_history`（変更履歴）・`audit_log`（行動ログ）は prompt.md の定義どおり
- 行動ログには**項目名だけ**書き、値（住所・電話など）は書かない

---

## 4. API（GAS doPost の action 一覧）

| action | 権限 | 内容 |
|---|---|---|
| login | なし | パスワード照合 → トークン発行（role, officer_id） |
| logout | 全員 | トークン破棄 |
| checkDuplicate | member | 社員コード＋生年月日が登録済みか（true/false のみ返す） |
| identify | member | 社員コード＋生年月日 → 本人の台帳を返す |
| submitCreate | member | 新規登録（申請） |
| submitUpdate | member | 更新（申請） |
| myRequests / cancelRequest | member | 自分の申請状況・取消 |
| listMembers | officer | 行（あ〜わ）・氏名検索・ページ → 社員番号/氏名/カナ/年齢/更新日のみ（未登録＝承認待ちの新規は出さない） |
| getMember | officer | 1人分の全項目＋家族（毎回ログ記録） |
| listRequests / approve / reject | officer | 承認待ち・承認・却下 |
| deleteMember / restoreMember | officer | ゴミ箱（論理削除・復元） |
| listLogs | officer | 行動ログ（絞り込み・50件ずつ） |

- 全actionの先頭で「トークン検証 → 権限チェック」。失敗は audit_log に記録
- `checkDuplicate`/`identify` は1トークンあたりの回数制限をかける（他人の社員コード・生年月日の総当たり対策）

---

## 5. ファイル構成

```
member-portal/
├── docs/                      # GitHub Pages 公開ディレクトリ
│   ├── index.html             # 1ページ構成（画面は JS で切替）
│   └── assets/
│       ├── api.js             # GAS_URL 定数と fetch 共通処理
│       ├── app.js             # 画面切替・フォーム・一覧
│       ├── ledger.js          # 台帳表示（PDF風レイアウト）の描画
│       └── style.css          # 印刷用CSS・台帳の罫線など
├── gas/
│   ├── Code.gs  auth.gs  members.gs  requests.gs  trash.gs
│   ├── audit.gs  line.gs  backup.gs  appsscript.json
├── DESIGN.md                  # この設計書
├── README.md / SECURITY.md
└── .gitignore
```

---

## 6. LINE通知（おすすめ構成）

- **Webhookなし・pushのみ**。GASの時間トリガーで1日1回、「承認待ちの申請が N 件あります」だけを役員グループへ送信（氏名など個人情報は書かない）
- 新しい承認待ちが増えた日だけ送る（毎日同じ通知を繰り返さない）
- Bot最大5個のローテーション・残量確認・Retry-Key は prompt.md どおり
- 実装は後半フェーズ（画面とGASの本体が動いてから）

---

## 7. 進める順番（案）

1. **設計確定**（このドキュメント）
2. **HTMLモック**（ダミーデータで画面だけ動く状態：ログイン → 新規/更新 → 管理一覧 → 台帳表示 → ログ）
3. GAS：認証 → 新規/更新 → 一覧/詳細 → ログ
4. 画面とGASの接続
5. 承認フロー → ゴミ箱 → バックアップ → LINE通知
6. README / SECURITY.md、セキュリティ自己レビュー

---

## 8. 追加で決まったこと（v0.2）

| # | 項目 | 決定 |
|---|---|---|
| Q1 | 数字・社員番号 | 数字はすべて半角（全角で入れても自動で半角に直す）。社員番号は **7桁** |
| - | 日付の入力 | 西暦で「年［    ］月［  ］日［  ］」の3つの欄。1でも01でも入力OK。保存は `1990-01-01`（ゼロ埋め）、表示は紙と同じ `1990年1月1日` |
| Q2 | 職名 | 自由入力 |
| Q3 | 続柄 | 妻・夫・子・父・母・義父・義母・その他 |
| Q4 | 性別 | 男・女 |
| Q5 | 印刷 | 台帳表示に［印刷（A4）］ボタンを付ける |
| Q6 | 一覧の列 | **社員番号・氏名・フリガナ・年齢・更新日** |
| Q7 | 必須項目 | 3-1 の表のとおり |
| Q8 | 更新画面 | 本人確認OKなら現在の内容を表示（5回失敗で15分ロック・ログ記録で抑止） |

## 9. HTMLモック

- デザインキャンバス（Claude アーティファクト）で試作：ログイン／組合員（新規・更新・確認・完了）／管理（一覧・承認待ち・行動ログ）／台帳表示
- 試作データ：社員番号 `1234567`＋生年月日 `1990年4月12日` が登録済み（架空）

## 10. 追加で決まったこと（v0.3）

- **スマホ・PC 両対応**：全画面を同じHTMLで、画面幅 900px を境に切り替え
  - 管理画面：PCは左サイドバー＋表、スマホは下タブ（台帳一覧／承認待ち／行動ログ）＋カード表示
  - 台帳表示：スマホでは紙の見た目のまま縮小表示し、［拡大して見る］で原寸（横スクロール）
- **確認画面＝台帳表示**：新規・更新とも、入力後は紙の台帳と同じ見た目で確認 → 下の［この内容で申請する］で申請。更新は変更箇所を黄色で表示
- 確認画面・完了画面の文言：「間違えた場合は、もう一度申請すれば前回の申請は破棄され、新しい内容に更新されます」
- 新規の本人確認で「未登録です」の表示には年齢を出さない
- 本人確認の生年月日は数値で比較するので、月日は「1」でも「01」でも一致する
- **スマホの台帳表示は2つのボタンで切り替え**：［読みやすい表示］（項目を縦に並べたカード形式・初期表示）／［台帳の形］（紙と同じ見た目・拡大可）。確認画面と管理の台帳表示の両方に適用。更新時の変更箇所はどちらの表示でも黄色
